/** Wysokość fixed hero podglądu na mobile (V2) */
export const CONFIGURATOR_V2_MOBILE_PREVIEW_HEIGHT =
  "h-[38vh] min-h-[240px] max-h-[320px] sm:h-[40vh] sm:min-h-[260px] sm:max-h-[340px]"

/** Powiększenie renderu dywanika w ramce (scale-100 = bez powiększenia) */
export const CONFIGURATOR_V2_MOBILE_MAT_IMAGE_SCALE = "scale-100"

/** Odstęp treści pod fixed podglądem mobilnym (navbar + wysokość hero) */
export const CONFIGURATOR_V2_MOBILE_CONTENT_PT =
  "pt-[calc(4rem+min(320px,max(240px,38vh)))] sm:pt-[calc(4rem+min(340px,max(260px,40vh)))] md:pt-[calc(5rem+min(340px,max(260px,40vh)))]"
