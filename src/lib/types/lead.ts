export interface LeadFormData {
  company?: string;
  jobTitle?: string;
  industry?: string;
  completeness?: string;
  borderColor?: string;
  materialColor?: string;
  // Add other properties as needed
}
